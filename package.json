{
  "name": "my-puppeteer-bot",
  "version": "1.0.0",
  "description": "A Puppeteer bot running on Render",
  "main": "auto-visit.js",
  "scripts": {
    "start": "npx puppeteer browsers install chrome && node auto-visit.js"
  },
  "dependencies": {
    "puppeteer": "^24.3.0"
  }
}